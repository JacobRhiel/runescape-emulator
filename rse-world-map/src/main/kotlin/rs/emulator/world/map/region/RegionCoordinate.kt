package rs.emulator.world.map.region

import rs.emulator.world.map.region.coordinate.Coordinate

/**
 *
 * @author Chk
 */
class RegionCoordinate(x: Int, z: Int, plane: Int) : Coordinate(x, z, plane)
{



}